package class1;

public class Student {
    // 클래스는 대문자로 시작, 카멜케이스로 씀
    // 각각의 변수는 멤버 변수 또는 필드라고 부른다.
    // 멤버 변수는 특정 클래스에 소속된 멤버이기 때문에 이렇게 부른다.
    // 필드는 데이터베이스 엑셀 등에서 데이터 각각의 항목을 필드라고 부른다.
    // 멤버변수는 자동으로 초기화 됨, 숫자는 0, boolean은 false, 참조값은 null
    String name;
    int age;
    int grade;
}
